name: ReleaseBuild
on:
  push: 
    branches:
      - master
env:
  # リポジトリのルートディレクトリを基点としたソリューションファイルのパス
  SOLUTION_FILE_PATH: Project/ONEngine.sln
  # visual studio の構成(Debug/Release)
  CONFIGURATION: Release

jobs:
  build:
    runs-on: windows-2022name: ReleaseBuild

on:
  push:
    branches:
      - master

env:
  SOLUTION_FILE_PATH: Project/ONEngine.sln
  CONFIGURATION: Release
  PREMAKE_SCRIPT_PATH: ./GenerateProject.ps1

jobs:
  build:
    runs-on: windows-2022

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Generate project with Premake
      shell: pwsh
      run: |
        Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned
        ./Project/GenerateProject.ps1

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2
      with:
        vs-version: 'latest'

    - name: Build ONEngine Release
      run: msbuild ${{ env.SOLUTION_FILE_PATH }} /p:Platform=x64,Configuration=${{ env.CONFIGURATION }}


    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Add MSBuild to PATH
        uses: microsoft/setup-msbuild@v2
        with:
          msbuild-architecture: x64
     
      - name: Build
        run: |
          msbuild ${{env.SOLUTION_FILE_PATH}} /p:Platform=x64,Configuration=${{env.CONFIGURATION}}
    
